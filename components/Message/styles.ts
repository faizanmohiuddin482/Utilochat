import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    positionMessage: {
        margin: 15
    },
    messageContainer: {
        backgroundColor: 'red',
        borderRadius: 50,
        alignItems: 'flex-end'
    },
    message: {
        padding: 10,
        margin: 5
    }
})

export default styles;